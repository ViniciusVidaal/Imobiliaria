import Image from "next/image"; import Link from "next/link"; import { BedDouble, CarFront, Maximize } from "lucide-react"; import { money } from "@/lib/constants"; import type { Property } from "@/lib/types";
export function PropertyCard({property}:{property:Property}){return <article className="property-card"><Link href={`/imovel/${property.slug||property.id}`} className="property-image"><Image src={property.images?.[0]||"https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?w=1200&q=80"} alt={property.title} fill sizes="(max-width: 768px) 50vw, 33vw" loading="lazy"/><span>{property.sold?"Vendido":property.transaction}</span></Link><div className="property-copy"><small>{property.type} · {property.location.split(",")[0]}</small><h3>{property.title}</h3><strong>{money(property.price)}</strong><div><span><BedDouble/> {property.bedrooms}</span><span><CarFront/> {property.parking}</span><span><Maximize/> {property.area} m²</span></div><Link href={`/imovel/${property.slug||property.id}`}>Ver detalhes <b>↗</b></Link></div></article>}
