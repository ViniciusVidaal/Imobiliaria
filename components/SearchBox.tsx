"use client"; import { Search } from "lucide-react"; import { useRouter } from "next/navigation"; import { FormEvent } from "react"; import { LOCATIONS,PROPERTY_TYPES } from "@/lib/constants";
export function SearchBox(){const router=useRouter();function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const q=new URLSearchParams(new FormData(e.currentTarget) as never);router.push(`/imoveis?${q}`)}return <form className="search-box" onSubmit={submit}><div className="transaction"><label><input type="radio" name="transaction" value="Compra" defaultChecked/> Comprar</label><label><input type="radio" name="transaction" value="Locação"/> Alugar</label></div><div className="search-grid"><label><span>O que você procura?</span><input name="keyword" placeholder="Ex.: 3 quartos, varanda..."/></label><label><span>Tipo de imóvel</span><select name="type" defaultValue=""><option value="">Todos os tipos</option>{PROPERTY_TYPES.map(x=><option key={x}>{x}</option>)}</select></label><label><span>Localização</span><select name="location" defaultValue=""><option value="">Todas as regiões</option>{LOCATIONS.map(x=><option key={x}>{x}</option>)}</select></label><label><span>Preço de</span><input type="number" name="min" placeholder="R$ 0"/></label><label><span>Preço até</span><input type="number" name="max" placeholder="Sem limite"/></label><button className="btn primary"><Search/> Buscar imóveis</button></div></form>}
