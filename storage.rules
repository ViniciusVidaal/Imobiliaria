rules_version = '2';
service firebase.storage { match /b/{bucket}/o { match /properties/{fileName} { allow read: if true; allow write: if request.auth != null && request.resource.size < 300 * 1024 && request.resource.contentType == 'image/webp'; } } }
