"use client"; import Image from "next/image"; import { useParams } from "next/navigation"; import { useEffect,useState } from "react"; import { Bath, BedDouble, CarFront, Check, MapPin, Maximize, MessageCircle } from "lucide-react"; import { Header } from "@/components/Header"; import { Fabs } from "@/components/Fabs"; import { subscribeProperties } from "@/lib/properties"; import { money } from "@/lib/constants"; import type { Property } from "@/lib/types";
export default function PropertyPage(){const {slug}=useParams<{slug:string}>(),[property,setProperty]=useState<Property|null>();useEffect(()=>subscribeProperties(items=>setProperty(items.find(p=>p.slug===slug||p.id===slug)||null)),[slug]);if(property===undefined)return <div className="detail-loading">Carregando imóvel...</div>;if(!property)return <><Header/><div className="detail-loading"><h1>Imóvel não encontrado</h1><p>Este anúncio pode ter sido removido ou vendido.</p></div></>;const photos=property.images?.length?property.images:["https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?w=1800&q=85"];return <><Header/><main className="detail-page"><section className="detail-heading"><div><span>{property.type} · {property.transaction}</span><h1>{property.title}</h1><p><MapPin/> {property.address||property.location}</p></div><strong>{property.sold?"Imóvel vendido":money(property.price)}</strong></section><section className="gallery-detail">{photos.slice(0,5).map((src,i)=><div key={src+i}><Image src={src} alt={`${property.title} — foto ${i+1}`} fill priority={i===0} loading={i===0?"eager":"lazy"} sizes={i===0?"(max-width: 768px) 100vw, 66vw":"(max-width: 768px) 50vw, 33vw"}/></div>)}</section><section className="detail-content"><article><div className="features"><span><BedDouble/><b>{property.bedrooms}</b> quartos</span><span><Bath/><b>{property.bathrooms}</b> banheiros</span><span><CarFront/><b>{property.parking}</b> vagas</span><span><Maximize/><b>{property.area}</b> m²</span></div><h2>Sobre este imóvel</h2><p>{property.description}</p><h2>Diferenciais</h2><div className="checks"><span><Check/> Atendimento personalizado</span><span><Check/> Documentação verificada</span><span><Check/> Visita com hora marcada</span></div></article><aside><span>Gostou deste imóvel?</span><h2>Agende uma visita.</h2><p>Nossa equipe entra em contato para esclarecer suas dúvidas e encontrar o melhor horário.</p><button className="btn primary"><MessageCircle/> Falar com um especialista</button><small>Código do imóvel: {property.id.slice(0,8).toUpperCase()}</small></aside></section></main><Fabs/></>}
